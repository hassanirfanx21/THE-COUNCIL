# ── Backend .env.example ──────────────────────────────────────
# Copy this to .env and fill in your values
# Never commit .env to git!

DB_URI=postgresql://langchain:langchain@localhost:5432/langgraph_mem
SSL_MODE=disable
GROQ_API_KEY=gsk_your_groq_key_here
